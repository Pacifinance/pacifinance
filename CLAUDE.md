@AGENTS.md

All shared project rules live in `AGENTS.md` (imported above) so Claude
Code, GitHub Copilot, and other tools stay in sync instead of drifting.
Nothing Claude Code needs is different from that file today — if that
changes, add Claude-specific notes below this line rather than editing
`AGENTS.md`.
